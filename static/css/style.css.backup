* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    color: #333;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

header {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 15px;
    padding: 20px;
    margin-bottom: 20px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
}

header h1 {
    font-size: 2rem;
    background: linear-gradient(45deg, #667eea, #764ba2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 5px;
}

.branding {
    text-align: center;
}

.branding .tagline {
    font-size: 1rem;
    color: #666;
    font-weight: 500;
    margin-top: 5px;
}

.system-info {
    display: flex;
    gap: 15px;
    font-size: 0.9rem;
    color: #666;
}

.system-info span {
    background: rgba(102, 126, 234, 0.1);
    padding: 5px 10px;
    border-radius: 20px;
    font-weight: 500;
}

main {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
}

section {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 15px;
    padding: 20px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

section h2 {
    margin-bottom: 15px;
    color: #333;
    font-size: 1.3rem;
    border-bottom: 2px solid #667eea;
    padding-bottom: 10px;
}

.stream-controls {
    grid-column: span 2;
}

.form-group {
    margin-bottom: 15px;
}

.form-group label {
    display: block;
    margin-bottom: 5px;
    font-weight: 500;
    color: #555;
}

.form-group input,
.form-group select {
    width: 100%;
    padding: 10px;
    border: 2px solid #e1e5e9;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.3s;
}

.form-group input:focus,
.form-group select:focus {
    outline: none;
    border-color: #667eea;
}

.form-actions {
    display: flex;
    gap: 10px;
    margin-top: 20px;
}

button {
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s;
}

#start-btn {
    background: linear-gradient(45deg, #667eea, #764ba2);
    color: white;
}

#start-btn:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
}

#stop-btn {
    background: #e74c3c;
    color: white;
}

#stop-btn:hover:not(:disabled) {
    background: #c0392b;
    transform: translateY(-2px);
}

button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.video-container {
    grid-column: span 1;
}

.video-wrapper {
    position: relative;
    background: #000;
    border-radius: 10px;
    overflow: hidden;
    aspect-ratio: 16/9;
    margin-bottom: 10px;
}

#video-player {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.placeholder {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #1a1a1a;
}

.placeholder-content {
    text-align: center;
    color: #666;
}

.placeholder .icon {
    font-size: 3rem;
    margin-bottom: 10px;
}

.stream-info {
    display: flex;
    justify-content: space-between;
    font-size: 0.9rem;
    color: #666;
}

.streams-list {
    grid-column: span 1;
}

.stream-item {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 10px;
    border-left: 4px solid #667eea;
}

.stream-item.error {
    border-left-color: #e74c3c;
}

.stream-item.playing {
    border-left-color: #27ae60;
}

.stream-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 5px;
}

.stream-id {
    font-weight: 600;
    color: #333;
}

.stream-status {
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 0.8rem;
    font-weight: 500;
}

.stream-status.playing {
    background: #d4edda;
    color: #155724;
}

.stream-status.stopped {
    background: #f8d7da;
    color: #721c24;
}

.stream-status.starting {
    background: #fff3cd;
    color: #856404;
}

.stream-url {
    font-size: 0.9rem;
    color: #666;
    margin-bottom: 5px;
}

.stream-duration {
    font-size: 0.8rem;
    color: #999;
}

.no-streams {
    text-align: center;
    color: #999;
    padding: 20px;
}

.logs {
    grid-column: span 2;
    max-height: 300px;
}

#log-container {
    height: 250px;
    overflow-y: auto;
    background: #1a1a1a;
    border-radius: 8px;
    padding: 10px;
    font-family: 'Courier New', monospace;
    font-size: 0.85rem;
}

.log-entry {
    margin-bottom: 5px;
    padding: 2px 5px;
    border-radius: 3px;
}

.log-entry.info {
    color: #17a2b8;
}

.log-entry.success {
    color: #28a745;
}

.log-entry.warning {
    color: #ffc107;
}

.log-entry.error {
    color: #dc3545;
}

/* Network Status Styles */
.network-status {
    grid-column: span 2;
}

.network-info {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 20px;
    margin-bottom: 20px;
}

.network-interfaces h3,
.network-stats h3 {
    margin-bottom: 15px;
    color: #333;
    font-size: 1.1rem;
}

.interface-placeholder,
.scan-placeholder {
    text-align: center;
    color: #999;
    padding: 20px;
    background: #f8f9fa;
    border-radius: 8px;
}

.interface-item {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 10px;
    border-left: 4px solid #667eea;
}

.interface-item.down {
    border-left-color: #dc3545;
    opacity: 0.7;
}

.interface-name {
    font-weight: 600;
    color: #333;
    margin-bottom: 5px;
}

.interface-details {
    font-size: 0.9rem;
    color: #666;
}

.interface-details span {
    margin-right: 15px;
}

.bandwidth-display {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 15px;
}

.bandwidth-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 10px;
}

.bandwidth-item:last-child {
    margin-bottom: 0;
}

.bandwidth-item .label {
    color: #666;
}

.bandwidth-item span:last-child {
    font-weight: 600;
    color: #333;
}

.network-controls {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
}

/* Network Configuration Styles */
.network-config {
    grid-column: span 2;
}

.config-tabs {
    display: flex;
    margin-bottom: 20px;
    border-bottom: 2px solid #e1e5e9;
}

.tab-btn {
    background: none;
    border: none;
    padding: 10px 20px;
    cursor: pointer;
    font-size: 1rem;
    color: #666;
    border-bottom: 2px solid transparent;
    transition: all 0.3s;
}

.tab-btn.active {
    color: #667eea;
    border-bottom-color: #667eea;
}

.tab-btn:hover {
    color: #667eea;
}

.tab-content {
    display: none;
}

.tab-content.active {
    display: block;
}

.wifi-networks {
    max-height: 300px;
    overflow-y: auto;
}

.wifi-network-item {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    transition: background-color 0.3s;
}

.wifi-network-item:hover {
    background: #e9ecef;
}

.wifi-network-item.encrypted::after {
    content: "🔒";
    margin-left: 5px;
}

.wifi-ssid {
    font-weight: 600;
    color: #333;
}

.wifi-details {
    font-size: 0.9rem;
    color: #666;
}

.wifi-signal {
    text-align: right;
}

.signal-strength {
    font-weight: 600;
    color: #333;
}

.signal-quality {
    font-size: 0.8rem;
    color: #666;
}

.ethernet-config,
.hotspot-config {
    max-width: 500px;
}

.ethernet-config .form-group,
.hotspot-config .form-group {
    margin-bottom: 15px;
}

.ethernet-config label,
.hotspot-config label {
    display: block;
    margin-bottom: 5px;
    font-weight: 500;
    color: #555;
}

.ethernet-config input,
.ethernet-config select,
.hotspot-config input,
.hotspot-config select {
    width: 100%;
    padding: 10px;
    border: 2px solid #e1e5e9;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.3s;
}

.ethernet-config input:focus,
.ethernet-config select:focus,
.hotspot-config input:focus,
.hotspot-config select:focus {
    outline: none;
    border-color: #667eea;
}

.static-config {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 15px;
    margin-top: 15px;
}

.hotspot-status {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 20px;
    padding: 15px;
    background: #f8f9fa;
    border-radius: 8px;
}

.hotspot-status span {
    font-weight: 600;
    color: #333;
}

/* Button Styles */
.btn-primary,
.btn-secondary {
    padding: 10px 20px;
    border: none;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s;
}

.btn-primary {
    background: linear-gradient(45deg, #667eea, #764ba2);
    color: white;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
}

.btn-secondary {
    background: #6c757d;
    color: white;
}

.btn-secondary:hover {
    background: #5a6268;
    transform: translateY(-2px);
}

/* Footer Styles */
footer {
    background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
    color: white;
    margin-top: 40px;
    padding: 30px 0;
    border-top: 3px solid #667eea;
}

.footer-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 20px;
}

.footer-branding {
    text-align: center;
}

.footer-branding .logo {
    font-size: 1.5rem;
    font-weight: 700;
    color: #667eea;
    margin-bottom: 5px;
}

.footer-branding .tagline {
    font-size: 0.9rem;
    color: #bdc3c7;
}

.footer-info {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
}

.footer-info .company {
    margin-bottom: 10px;
}

.footer-info .copyright {
    font-size: 0.8rem;
    color: #95a5a6;
    margin-top: 5px;
}

.footer-info .version {
    font-size: 0.8rem;
    color: #667eea;
    font-weight: 500;
}

/* Responsive Design */
@media (max-width: 768px) {
    main {
        grid-template-columns: 1fr;
    }
    
    .stream-controls,
    .network-status,
    .network-config,
    .logs {
        grid-column: span 1;
    }
    
    .network-info {
        grid-template-columns: 1fr;
    }
    
    .network-controls {
        justify-content: center;
    }
    
    .config-tabs {
        overflow-x: auto;
    }
    
    .tab-btn {
        white-space: nowrap;
    }
    
    header {
        flex-direction: column;
        text-align: center;
        gap: 10px;
    }
    
    .system-info {
        justify-content: center;
    }
    
    .footer-content {
        flex-direction: column;
        text-align: center;
        gap: 15px;
    }
    
    .footer-branding,
    .footer-info {
        width: 100%;
    }
}

@media (max-width: 768px) {
    main {
        grid-template-columns: 1fr;
    }
    
    .stream-controls,
    .logs {
        grid-column: span 1;
    }
    
    header {
        flex-direction: column;
        text-align: center;
        gap: 10px;
    }
    
    .system-info {
        justify-content: center;
    }
}
